[project]
name = "blog-downloader"
version = "0.1.0"
description = "A tool for download all blogs from website to markdown file"
authors = [
    {name = "hx",email = "im@bgzo.cc"}
]
license = {text = "AGPL-3.0"}
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
